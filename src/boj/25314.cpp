#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;

    for (int i = 4; i <= n; i += 4) {
        cout << "long" << ' ';
    }
    cout << "int" << '\n';

    return 0;
}

// 1 min. 30 

